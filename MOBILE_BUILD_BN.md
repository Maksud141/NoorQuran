# 📱 মোবাইল দিয়ে APK Build

1. এই project GitHub repository-তে upload করুন।
2. GitHub → Actions → **Build Android APK** খুলুন।
3. **Run workflow** চাপুন।
4. Build শেষ হলে workflow-এর Artifacts থেকে **NoorQuran-debug-apk** download করুন।
5. ZIP extract করে `app-debug.apk` ফোনে install করুন।

আগের UI/source files অপরিবর্তিত রাখা হয়েছে; শুধু GitHub Actions workflow যোগ করা হয়েছে।
